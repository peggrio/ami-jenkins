jenkins:
  systemMessage: "This Jenkins Instance is managed by CSYE7125 Summer2024 group 04"
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "admin"
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:admin"
        - "Overall/Read:authenticated"
        - "Overall/Read:anonymous"
